import { SituacaoPipe } from '../app/shared/pipes/situacao.pipe';

describe('SituacaoPipe', () => {
  it('create an instance', () => {
    const pipe = new SituacaoPipe();
    expect(pipe).toBeTruthy();
  });
});
